# Copy to .env to override. Defaults (in src/config.js) match local dev.

# Backend API base URL. Keep in sync with the backend's CORS origins.
VITE_API_BASE_URL=http://localhost:8000

# Auto-compile delay after typing stops (milliseconds).
VITE_DEBOUNCE_MS=3000
